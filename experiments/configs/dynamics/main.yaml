defaults:
  - mosvgpe: main

_target_: moderl.dynamics.ModeRLDynamics
_convert_: "partial"
state_dim: 2
desired_mode: 0
learning_rate: 0.01
epsilon: 1.0e-08
dynamics_fit_kwargs:
  batch_size: 64
  epochs: 20000
  verbose: true
  validation_split: 0.0
