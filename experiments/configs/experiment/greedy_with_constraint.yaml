# @package _global_
wandb:
  exploration: "greedy"
  name: "greedy_with_constraint"

controller:
  explorative_objective_fn: null

training:
  num_episodes: 20 # because it gets stuck in local optima
