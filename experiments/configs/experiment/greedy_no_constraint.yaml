# @package _global_
# defaults:
# - override /controller: no-constraints
# - override /controller: main
# - override /controller: greedy-no-constraint
# - override /server: nginx

wandb:
  exploration: "greedy"
  name: "greedy-no-constraint"

controller:
  explorative_objective_fn:
    _target_: moderl.objectives.build_bernoulli_entropy
    dynamics: ${dynamics}
    start_state: ${start_state}
  mode_satisfaction_prob: 0.0
